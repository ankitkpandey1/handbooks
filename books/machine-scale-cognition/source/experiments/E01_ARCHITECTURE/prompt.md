You are evaluating six rival architectures for a short technical field guide about how one expert can exploit scalable AI cognition. This is an adversarial failure-search experiment, not a request to harmonize or praise them.

Read the candidate definitions and frozen tasks below. For every architecture A0–A5 and every task T1–T3, produce a compact first-48-hour operating plan that follows that architecture's governing logic. Each plan must state: machine-scale workload, concrete first action, external selector/falsifier, maximum human review object, and stop/authority boundary.

Then:

1. score each architecture/task pair from 0–4 on executable next action, scalable workload, external selection, bounded human review, and stop/authority boundary;
2. quote or precisely point to the plan feature supporting each nonzero score;
3. name the strongest disqualifying failure for each architecture;
4. test whether each architecture remains operationally distinguishable from A0 after labels are removed;
5. select a provisional winner, runner-up, and architectures that should be eliminated;
6. do not invent experiment outcomes, sources, costs, or evidence.

Output Markdown. Separate generated plans from evaluative judgments. Treat the six-chapter limit as fixed but do not assume A0 is correct.

--- CANDIDATES ---

A0 inherited lifecycle: leverage → compile → expand → select → act → compound.
A1 closed-loop control: specify target/sensors → model state → intervene → observe error → adapt policy → redesign controller.
A2 bottleneck demolition: find binding constraint → manufacture machine work → manufacture discriminator → compress crossing → remove next constraint → retain capability.
A3 cognitive-options portfolio: create options → price information → diversify mechanisms → stage reversible bets → exercise under evidence → reinvest learning.
A4 adversarial cognitive market: commission generators → force heterogeneous bids → fund falsifiers → clear against evidence → execute bounded winner → update reputations/budgets.
A5 evidence compiler: compile objective into claims/tests → build evidence graph → execute search/computation → reject nodes → emit bounded decision → recompile from outcomes.

--- TASKS ---

T1: A materials scientist has one week to decide which of 400 papers and 30 plausible mechanisms justify three expensive experiments on why a new electrolyte loses conductivity after cycling. Evidence is heterogeneous and some papers reuse the same datasets.

T2: A small B2B software company must decide whether to launch a free tier while two better-funded competitors can respond. It has six weeks, limited sales data, and can run only two reversible market tests.

T3: A university dean asks one analyst to publish a policy for student use of generative AI next semester. Evidence about learning effects is weak and fast-changing; faculty values conflict; enforcement can harm students.
